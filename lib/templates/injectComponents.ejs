<% components.forEach(function (component) { -%>
import <%= component.name %> from '<%= component.relativePath -%>'
import <%= component.name %>Config from '<%= component.configRelativePath %>'
<% }) -%>

<% components.forEach(function (component) { -%>
<%= component.name %>Config.tagName = '<%= component.tagName %>'
<%= component.name %>Config.name = '<%= component.name %>'
Vue.component('<%= component.tagName %>', <%= component.name %>)
<% }) -%>



export default [
<% components.forEach(function (component) { -%>
  <%= component.name %>Config,
<% }) -%>
]